# Perfil mariadb: DB_ENGINE=mariadb. Crear antes la base:  CREATE DATABASE edumanager;
spring.datasource.url=${DB_URL:jdbc:mariadb://127.0.0.1:3306/edumanager}
spring.datasource.username=${DB_USER:root}
spring.datasource.password=${DB_PASSWORD:root123}
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver
